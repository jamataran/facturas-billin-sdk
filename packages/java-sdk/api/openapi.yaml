openapi: 3.0.0
info:
  contact:
    name: Facturas Billin
    url: https://www.facturasbillin.net/
  description: API REST de Facturas Billin - PLACEHOLDER - Reemplaza este archivo
    con la especificación OpenAPI/Swagger oficial de facturasbillin.net
  title: Facturas Billin API
  version: 1.0.0
servers:
- description: Servidor de producción
  url: https://api.facturasbillin.net
security:
- ApiKeyAuth: []
paths:
  /ping:
    get:
      description: Endpoint de ejemplo para verificar que la API está funcionando
      operationId: ping
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ping_200_response'
          description: Respuesta exitosa
      summary: Health check endpoint
      x-accepts: application/json
components:
  schemas:
    ping_200_response:
      example:
        status: ok
      properties:
        status:
          example: ok
          type: string
      type: object
  securitySchemes:
    ApiKeyAuth:
      in: header
      name: Authorization
      type: apiKey

